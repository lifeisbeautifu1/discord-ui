import { Header, Hero } from '../components';

const Home = () => {
  return (
    <div className="scrollbar-hide overflow-hidden">
      <Header />
      <Hero />
    </div>
  );
};

export default Home;
